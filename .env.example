# Copy to .env for docker compose (or set in dev.env and export before docker compose up)
POSTGRES_DB=stockdb
POSTGRES_USER=stock
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432

# Spring / Alpha Vantage (used when running the app locally)
ALPHA_VANTAGE_API_KEY=
